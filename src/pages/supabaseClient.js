// src/pages/supabaseClient.js
// Dummy page so Next.js is happy. The real Supabase client lives in /lib.

export const supabase = null;

export default function SupabaseClientPage() {
  // This page isn't meant to be visited directly.
  return null;
}
